import 'package:flutter/material.dart';

/// Color constants for the dark mode admin theme
class AppColors {
  // Primary Colors
  static const Color primaryColor = Color(0xFF6366F1); // Indigo
  static const Color primaryDark = Color(0xFF4F46E5);
  static const Color primaryLight = Color(0xFF818CF8);

  // Background Colors
  static const Color backgroundColor = Color(0xFF0F172A); // Dark slate
  static const Color surfaceColor = Color(0xFF1E293B);
  static const Color cardColor = Color(0xFF334155);

  // Text Colors
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFFCBD5E1);
  static const Color textMuted = Color(0xFF94A3B8);

  // Status Colors
  static const Color successColor = Color(0xFF10B981);
  static const Color errorColor = Color(0xFFEF4444);
  static const Color warningColor = Color(0xFFF59E0B);
  static const Color infoColor = Color(0xFF3B82F6);

  // Accent Colors
  static const Color hotDealColor = Color(0xFFEC4899); // Pink for hot deals
  static const Color soldOutColor = Color(0xFF6B7280); // Gray for sold out

  // Border & Divider
  static const Color borderColor = Color(0xFF475569);
  static const Color dividerColor = Color(0xFF334155);

  // Sidebar
  static const Color sidebarBackground = Color(0xFF1E293B);
  static const Color sidebarHover = Color(0xFF334155);
  static const Color sidebarActive = Color(0xFF6366F1);
}
